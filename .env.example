# SEC requires a descriptive User-Agent ("Name email") or it returns HTTP 403.
SEC_USER_AGENT=deals-vectoriser research pushkarborkar1809@gmail.com

# Max requests/sec to SEC hosts (SEC ceiling is 10; stay conservative).
SEC_MAX_RPS=5

# Default lookback window (days) for the fetch job.
DEFAULT_LOOKBACK_DAYS=2

# ---- added in later stages ----
# OPENROUTER_API_KEY=
# SUPABASE_URL=
# SUPABASE_KEY=
